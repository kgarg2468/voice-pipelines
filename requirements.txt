# Company Brain — Python dependencies
rocketride==1.0.0      # RocketRide SDK — client that drives the pipelines on the local engine
neo4j>=5.20            # Neo4j driver — the planner's conflict graph (sync_neo4j.py)
